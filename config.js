// Client-side config is loaded before index.js so the API key is available globally.
// For a learning project this is fine, but in production an API key should usually be
// protected behind a backend or serverless function rather than exposed in browser code.
API_KEY = `92fb2c25`;