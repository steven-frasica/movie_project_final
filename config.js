// Client-side config is loaded before index.js so the API key is available globally.
// In production, sensitive keys should be proxied through a backend instead of shipped to the browser.
API_KEY = `92fb2c25`;